function getUpperCaseStrings() {

    var arr = ['permanent-internship', 'glutinous-shriek', 'multiplicative-elevation'];


    var result = [];

    for (var i = 0; i < arr.length; i++) {

        result.push(arr[i].toUpperCase());

    }

    document.write(result);
}